<%= form_for @course, remote: true do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_for @course, remote: true do |f| %>
  <h2>Create New Course</h2>
  <%= f.label :name %>(required)
  <%= f.text_field :name, class: 'form-control form-control-inline' %><br /><br />

  <%= f.label :time_of_day %>
  <%= f.text_field :time, class: 'timepicker form-control form-control-inline' %><br /><br />

  <%= f.label :period %>
  <%= f.text_field :period, class: 'form-control form-control-inline' %><br /><br />

  <%= f.label :section %>
  <%= f.text_field :section, class: 'form-control form-control-inline' %><br /><br />
  <div class="actions">
  <% if current_page? new_course_path %>
    <%= f.submit "Submit", class:"btn btn-primary " %>
  <% else %>
    <%= f.submit "Update", class:"btn btn-primary " %>
  <% end %>
  </div>
  <% end %>
<% end %>
