<% provide(:title, "Course") %>
<h2><%= @course.name %></h2>
<ul>
  <li><strong>Time:</strong> <%= handle_not_set @course.time %></li>
  <li><strong>Period:</strong> <%= handle_not_set @course.period %></li>
  <li><strong>Section:</strong> <%= handle_not_set @course.section %></li>
</ul>

<%= link_to 'Edit Course', edit_course_path(@course), class: "btn btn-primary " %> <br /><br />
<%= link_to 'Delete Course', course_path, method: :delete, data: { confirm: 'Are you sure you want to delete this course?' }, class: "btn btn-primary " %> <br /><br />
<%= link_to 'Back to Courses List', courses_path, class: "btn btn-primary " %><br /><br />
<h3>Enrolled Students</h3>
<div class="table-responsive">
<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Grade Level</th>
      <% @course.assignments.each do |assignment| %>
      <th><%= assignment.title %></th>
      <% end %>
    </tr>
  </thead>

  <tbody>
    <% @course.students.each do |student| %>
      <tr>
        <td><%= link_to student.first_name, student %></td>
        <td><%= student.last_name %></td>
        <td><%= student.grade_level %></td>
        <%#<% @student.assignments do |assignment| %>
        <%#<td><%= assignment.points_earned %>
        <%#<% end %>
      </tr>
    <% end %>
</tbody>
</table>
</div>

<%= link_to "Create New Assignment", new_course_assignment_route(@course.id), class: "btn btn-danger " %> <br /><br />

<%= link_to "Create and Enroll Student", new_enroll_student_route(@course.id), class: "btn btn-info " %> <br /><br />
