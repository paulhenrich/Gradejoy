<%= link_to "Edit #{@category.category}", edit_category_path(@category), class: "btn btn-danger" %><br /><br />
<%= link_to "Delete #{@category.category}", category_path, method: :delete, data: { confirm: 'Are you sure you want to delete this category?' }, class: "btn btn-primary" %>
