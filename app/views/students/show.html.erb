<h2><%= @student.first_name + ' ' + @student.last_name %></h2>
<div class="table-responsive">
  <table class="table table-bordered table-hover">
    <thead>
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Student ID</th>
        <th>Grade Level</th>
        <th>Birth Month</th>
        <th>Birth Day</th>
        <th>Birth Year</th>
      </tr>
    </thead>

    <tbody>
        <tr>
          <td><%= @student.first_name %></td>
          <td><%= @student.last_name %></td>
          <td><%= @student.id %></td>
          <td><%= @student.grade_level %></td>
          <td><%= @student.birth_month %></td>
          <td><%= @student.birth_year %></td>
        </tr>
    </tbody>
  </table>
</div>


<%= link_to 'Enroll Student in Course', student_path, class: "btn btn-primary btn-med", id: "createbutton" %> &nbsp -- Does not work yet<br /><br />
<%= link_to 'Edit Student Information', edit_student_path(@student), class: "btn btn-primary btn-med", id: "createbutton" %><br /><br />
<%= link_to 'Back to Created Students', students_path, class: "btn btn-primary btn-med", id: "createbutton" %><br /><br />
<%= link_to 'Delete Student', student_path, method: :delete, data: { confirm: 'Are you sure you want to delete this student?' }, class: "btn btn-primary btn-med", id: "createbutton" %></td>
