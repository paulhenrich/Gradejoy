<h3>Options for <%= @student.first_name + ' ' + @student.last_name %></h3>
<%= link_to 'Edit Student Information', edit_student_path(@student), class: "btn btn-primary", id: "edit_student", remote: true %><br /><br />
<%= link_to 'Remove Student', student_path, method: :delete, data: { confirm: 'Are you sure you want to delete this student?' }, class: "btn btn-primary" %>
