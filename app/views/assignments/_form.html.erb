<%= form_for(@assignment) do |f| %>
  <% if @assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% @assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_for @assignment do |a| %>
  <%= f.label :title %> (required)
  <%= f.text_field :title %><br /><br />

  <%= f.label :date_created %>
  <%= f.text_field :date_created %><br /><br />

  <%= f.label :date_due %> (required)
  <%= f.text_field :date_due %><br /><br />

  <%= f.label :category %> (required)
  <%= f.text_field :category %><br /><br />

  <%= f.label :total_points %> (required)
  <%= f.text_field :total_points %><br /><br />

  <div class="actions">
    <%= f.submit %>
  </div>

  <% end %>
<% end %>
