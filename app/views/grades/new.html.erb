<% provide(:title, "New Grade") %>
<h3>Create New Grade for <%= @student.first_name + ' ' + @student.last_name %>'s Assignment<% @assignment.title %> <% @course.name %> </h3>

<%= render 'form' %>
<br />
<%= link_to 'Back', course_path, class: "btn btn-primary" %>
